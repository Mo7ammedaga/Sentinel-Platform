# Copy to .env to override defaults (Create React App reads REACT_APP_* at build time).

# Backend base URL (REST + Socket.IO). Default: http://localhost:5000
REACT_APP_API_URL=http://localhost:5000

# Organization room to subscribe to for live alerts (single-org MVP).
REACT_APP_ORG_ID=org_001
