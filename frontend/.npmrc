# react-scripts 5 declares a legacy peer range for TypeScript (<=4), but the
# project builds and type-checks cleanly on TypeScript 5. Allow the install to
# resolve. (CRA is in maintenance; migrating to Vite would remove this.)
legacy-peer-deps=true
